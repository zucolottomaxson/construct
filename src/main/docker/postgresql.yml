version: '2'
services:
  construct-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/construct/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=construct
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
